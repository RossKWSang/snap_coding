import 'package:flutter/material.dart';

class SnapCardMain extends StatelessWidget {
  const SnapCardMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
